//
// Created by Alejandro  Otero on 2019-12-01.
//

#include "rotacion.h"

using namespace std;

/*void rotar(string_view filename, std::vector<unsigned char>& image, unsigned width, unsigned height){
    int x=cos(45)*width-sin(45)*height;
    int y=sin(45)*width+cos(45)*height;
    unsigned v=lodepng::encode(filename.data(),image,x,y);
    if(v){
        cout<<"encode error"<<v<<":"<<lodepng_error_text(v)<<endl;
    }
}*/